# Fastlane Match - sertifika ve provisioning profile'ları git repo'da tutar.
# İlk kurulum (bir kez, lokalde):
#   1. Boş bir private Git repo oluştur (örn. dahis-one-certs)
#   2. Aşağıdaki git_url'i kendi repo URL'inle değiştir
#   3. fastlane match appstore   # Sertifika + profile oluşturur, repo'ya push eder
#
# CI'da: match readonly: true ile sadece indirilir.

git_url(ENV["MATCH_GIT_URL"] || "https://github.com/KULLANICI/dahis-one-certs.git")
storage_mode("git")
type("appstore")
app_identifier(["com.dahis.app"])
username("y.emrecoskun24@gmail.com")
